#include <iostream>
#include <string>
#include <vector>
#include <cstring>

using namespace std;

typedef int int_array[4];

int main() {
    // int a[3][4];
    // int i=0;
    // for (auto &row : a)
    //     for (auto &col : row)
    //         col = ++i;
    // for (auto row : a[1])
    //     cout << row << ' ';
    // int_array k{0,1,2,3};
    // cout << k[0] << endl;
    bool b = 1;
    // cout << -7 % -2 << endl;
    const char cp[20] = "hello world";
    // cout << cp[0] << ' ' << cp << endl;
    int a[20]{0};
    // cout << a[10] << endl;
    // if (cp && *cp) ;
    // printf("%p", &cp);
    cout << &a << endl;
}





 